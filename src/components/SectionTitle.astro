---
const { title, subtitle } = Astro.props as { title: string; subtitle?: string };
---

<div class="mb-6">
  <h2 class="text-2xl font-semibold tracking-tight">{title}</h2>
  {subtitle && <p class="mt-2 text-black/70">{subtitle}</p>}
</div>
