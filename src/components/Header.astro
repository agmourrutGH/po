<header
  id="site-header"
  class="sticky top-0 z-40 border-b border-zinc-200 bg-white/70 backdrop-blur transition-all duration-300"
>
  <div class="mx-auto flex max-w-6xl items-center justify-between px-6 py-4">
    <!-- Logo -->
    <a href="#" class="font-semibold tracking-tight">
      Agustin Mourrut<span class="text-zinc-400">.</span>
    </a>

    <!-- Navigation (desktop) -->
    <nav class="hidden items-center gap-6 text-sm text-zinc-600 md:flex">
      <a href="#about" class="nav-link" data-en="About" data-es="Sobre mí">About</a>
      <a href="#education" class="nav-link" data-en="Education" data-es="Educación">Education</a>
      <a href="#skills" class="nav-link" data-en="Skills" data-es="Habilidades">Skills</a>
      <a href="#projects" class="nav-link" data-en="Projects" data-es="Proyectos">Projects</a>
      <a href="#experience" class="nav-link" data-en="Experience" data-es="Experiencia">Experience</a>
      <a href="#contact" class="nav-link" data-en="Contact" data-es="Contacto">Contact</a>
    </nav>

    <!-- Actions + Mobile Button -->
    <div class="flex items-center gap-3">
      <!-- 🔥 Botón menú mobile (con animación) -->
      <button
        id="menuBtn"
        class="md:hidden text-2xl transition hover:scale-110"
      >
        ☰
      </button>

      <!-- CV -->
      
        id="cvHeader"
        href="/cv-agustin-mourrut-en.pdf"
        target="_blank"
        rel="noopener"
        class="hidden sm:block rounded-xl border border-zinc-200 px-3 py-2 text-sm font-semibold hover:bg-zinc-50"
        data-en="CV"
        data-es="CV"
      >
        CV
      </a>

      <!-- idioma -->
      <button
        id="langToggle"
        class="rounded-xl border border-zinc-200 px-3 py-2 text-sm font-semibold hover:bg-zinc-50"
      >
        ES
      </button>
    </div>
  </div>

  <!-- 🔥 MOBILE MENU (con transición) -->
  <div
    id="mobileMenu"
    class="hidden flex-col gap-4 px-6 pb-4 text-sm text-zinc-600 md:hidden transition-all duration-300"
  >
    <a href="#about" data-en="About" data-es="Sobre mí">About</a>
    <a href="#education" data-en="Education" data-es="Educación">Education</a>
    <a href="#skills" data-en="Skills" data-es="Habilidades">Skills</a>
    <a href="#projects" data-en="Projects" data-es="Proyectos">Projects</a>
    <a href="#experience" data-en="Experience" data-es="Experiencia">Experience</a>
    <a href="#contact" data-en="Contact" data-es="Contacto">Contact</a>
  </div>
</header>